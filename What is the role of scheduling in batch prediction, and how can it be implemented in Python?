import schedule
import time

def batch_prediction():
    print("Running batch prediction...")

schedule.every().day.at("00:00").do(batch_prediction)

while True:
    schedule.run_pending()
    time.sleep(1)
